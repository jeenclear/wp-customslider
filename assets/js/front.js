jQuery( function() 
    {
    	var $params = jQuery;
        // scripts
         $params('.wp-customslider__pad').slick({
			dots: false,
			infinite: true,
			autoplay: true,
			draggable: false,
			autoplaySpeed: 4000
		});

    }
);